# Copy to `.env` in this folder (Screen 1)
VITE_BACKEND_URL=http://localhost:5000/api

# Optional — legacy Supabase client is unused; Postgres is configured on the backend only
# VITE_SUPABASE_URL=
# VITE_SUPABASE_PUBLISHABLE_KEY=
